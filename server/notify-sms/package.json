{
  "name": "notify-sms",
  "version": "1.0.0",
  "description": "Simple server to receive order webhooks and send SMS via Resend/Twilio",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
  ,"dependencies": {
    "@vonage/server-sdk": "^3.2.0",
    "node-fetch": "^3.4.1",
    "dotenv": "^16.0.0",
    "nodemailer": "^6.9.1",
    "resend": "^1.7.0"
  }
}
